# Accessibility Guidelines

1. **Semantic HTML**: Use standard elements (`<nav>`, `<main>`, `<button>`).
2. **ARIA**: Use `aria-labels` where visual context isn't sufficient.
3. **Keyboard**: Ensure full keyboard navigation support.
