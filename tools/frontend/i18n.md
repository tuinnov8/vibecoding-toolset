# Internationalization (i18n) Guidelines

1. **No Hardcoded Strings:** Never hardcode text strings directly into HTML, JSX, or templates.
2. **Translations:** Always extract strings to the localization files (e.g., `locales/en.json`).
3. **Hooks:** Use the appropriate localization hook or function (like `useTranslation()`) to render text in the UI.
